name: Build Textly APK

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Install dependencies
        run: flutter pub get

      # sms_advanced এবং Android compileSdk 34 ফিক্স করার স্ক্রিপ্ট
      - name: Fix Android SDK 34 and Package Issues
        run: |
          # 1. Fix sms_advanced package
          MANIFEST_FILE="$HOME/.pub-cache/hosted/pub.dev/sms_advanced-1.1.0/android/src/main/AndroidManifest.xml"
          GRADLE_FILE="$HOME/.pub-cache/hosted/pub.dev/sms_advanced-1.1.0/android/build.gradle"

          if [ -f "$MANIFEST_FILE" ]; then
            sed -i 's/package="com.elyudde.sms_advanced"//g' "$MANIFEST_FILE"
          fi

          if [ -f "$GRADLE_FILE" ]; then
            if ! grep -q "namespace" "$GRADLE_FILE"; then
              sed -i '/android {/a \    namespace "com.elyudde.sms_advanced"' "$GRADLE_FILE"
            fi
          fi

          # 2. Update app compileSdk and targetSdk to 34
          APP_GRADLE="android/app/build.gradle"
          if [ -f "$APP_GRADLE" ]; then
            sed -i 's/compileSdkVersion.*/compileSdkVersion 34/g' "$APP_GRADLE"
            sed -i 's/compileSdk.*/compileSdk 34/g' "$APP_GRADLE"
            sed -i 's/targetSdkVersion.*/targetSdkVersion 34/g' "$APP_GRADLE"
            sed -i 's/targetSdk.*/targetSdk 34/g' "$APP_GRADLE"
          fi

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Build APK
        run: flutter build apk --release

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Textly-Release-APK
          path: build/app/outputs/flutter-apk/app-release.apk
          retention-days: 1